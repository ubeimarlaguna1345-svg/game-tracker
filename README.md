# proyecto q10 / game-tracker

Este directorio ahora contiene un proyecto Vite + React (JavaScript).

Cómo arrancar:

```powershell
cd C:\repositorios\game-tracker
npm install
npm run dev
```

Si quieres que lo coloque en una subcarpeta en lugar de usar la raíz, dímelo.
